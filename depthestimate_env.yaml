name: depthestimate-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pytorch
  - torchvision
  - torchaudio
  - cudatoolkit=11.3
  - pip:
    - wheel
    - python-dotenv
    - tensorboardx
    - scikit-image
    - matplotlib
    - tensorboard
    - albumentation

