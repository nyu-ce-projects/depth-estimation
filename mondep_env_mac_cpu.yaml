name: mondep-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - torch
    - torchvision
    - torchaudio
    - python-dotenv
    - tensorboardX
    - scikit-image

